/**
 * Setup for the node-environment server test project.
 *
 * Nothing global yet — the harness (./harness.ts) is imported per suite. This
 * file exists so vitest has a stable hook point (and so future global setup —
 * fake timers, env defaults — has a home).
 */
export {};
